spring.application.name = servicio-items

# Primero se ejecutara este archivo de Arranque (Bootstrap) para buscar la configuracion del Repo Git 
# y luego se registra en Eureka. Luego se ejecuta el application.properties
spring.cloud.config.uri=http://localhost:8888
